@model List<Target>
@Html.DropDownList("IDTarget", Model.Select(x => new SelectListItem() { Text = x.Name, Value = x.ID.ToString() }).ToList(), Locate.T("--- Chọn chỉ tiêu ---"), new { @class = "form-control input-sm" })
